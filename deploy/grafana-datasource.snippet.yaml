# Добавить в monitoring/grafana/configmaps.yaml рядом с "Life Dashboard PostgreSQL"
      - name: Citroen C4
        uid: citroen-postgres
        type: postgres
        url: postgres-rw.citroen.svc.cluster.local:5432
        user: car
        jsonData:
          database: car
          sslmode: disable
          postgresVersion: 1600
        secureJsonData:
          password: $CITROEN_PG_PASSWORD
